name: Build Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '25'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v2

      - name: Create .env file
        run: |
          echo "EXPO_PUBLIC_API_URL=${{ secrets.EXPO_PUBLIC_API_URL || 'https://gosharebd.com' }}" > .env

      - name: Prebuild Android
        run: npx expo prebuild --platform android --clean

      - name: Create Google-services.json placeholder
        run: |
          if [ -f "android/app/google-services.json" ]; then
            echo "google-services.json already exists"
          else
            echo '{}' > android/app/google-services.json
          fi

      - name: Build Release APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease
          ls -la app/build/outputs/apk/release/

      - name: Get version name
        id: version
        run: |
          VERSION_NAME=$(node -p "require('./app.json').expo.version")
          echo "version=$VERSION_NAME" >> $GITHUB_OUTPUT
          echo "Version: $VERSION_NAME"

      - name: Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: gosharebd-v${{ steps.version.outputs.version }}-apk
          path: android/app/build/outputs/apk/release/app-release.apk
          retention-days: 30

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          body: |
            GoShareBD Mobile App v${{ steps.version.outputs.version }}
            
            Install this APK on your Android device to test the app.
            
            ### Features
            - Browse tour packages
            - View package details
            - Account management
            - And more...
          files: android/app/build/outputs/apk/release/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
